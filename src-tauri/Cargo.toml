[package]
name = "recline-desktop"
version = "0.1.0"
description = "Recline — privacy-first chat, calls, communities (desktop)"
authors = ["Recline contributors"]
edition = "2021"
license = "AGPL-3.0-only"

# binary the `cargo run` / `tauri dev` actually executes
[[bin]]
name = "recline-desktop"
path = "src/main.rs"

[lib]
name = "recline_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-shell = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# Smaller release binaries
[profile.release]
panic = "abort"
codegen-units = 1
lto = true
opt-level = "s"
strip = true
